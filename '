// main binds

// jump throw on N
alias "+jumpaction" "+jump;"
alias "+throwaction" "-attack; -attack2"
alias "-jumpaction" "-jump"
bind "n" "+jumpaction;+throwaction;"

// running jump throw on M (trying this out)
alias "+runthrow" "+forward;+jump;"
alias "-runthrow" "-jump;-forward"
bind "m" "+runthrow;+throwaction"

bind "0" "give weapon_hegrenade;give weapon_flashbang;give weapon_smokegrenade;give weapon_molotov"
bind "1" "slot1"
bind "2" "slot2"
bind "3" "slot3"
bind "4" "slot4"
bind "5" "slot5"

bind "w" "+forward"
bind "a" "+left"
bind "s" "+back"
bind "d" "+right"
bind "MWHEELDOWN" "+jump"

bind "CTRL" "+duck"
bind "SHIFT" "+sprint"

bind "MWHEELUP" "use weapon_knife;use weapon_flashbang"
bind "z" "use weapon_knife;use weapon_hegrenade"
bind "x" "use weapon_knife;use weapon_molotov;use weapon_incgrenade"
bind "c" "use weapon_knife;use weapon_smokegrenade"

bind "f" "+lookatweapon; r_cleardecals"
bind "v" "+voicerecord"
bind "MOUSE4" "+voicerecord"

bind "b" "buymenu"
bind "e" "+use"
bind "g" "drop"
bind "h" "show"
bind "i" "show_loadout_toggle"
bind "m" "teammenu"
bind "q" "lastinv"
bind "r" "+reload"
bind "t" "+spray_menu"
bind "u" "messagemode2"
bind "y" "messagemode"
bind "\" "slot12;sv_rethrow_last_grenade"
bind "`" "toggleconsole"

bind "," "buyammo1"
bind "." "buyammo2"
bind "SPACE" "+jump"
bind "TAB" "+showscores"
bind "ESCAPE" "cancelselect"
bind "DEL" "mute"
bind "PAUSE" "pause"
bind "ALT" "noclip"
bind "MOUSE1" "+attack"
bind "MOUSE2" "+attack2"

// viewmodel conf
viewmodel_fov 68;
viewmodel_offset_x 2.5; 
viewmodel_offset_y 2.0; 
viewmodel_offset_z -2.5; 

// crosshair (black, almost transparent, with dot)
cl_crosshairstyle 4;
cl_crosshaircolor 5;
cl_crosshaircolor_r 0.00;
cl_crosshaircolor_g 0.00;
cl_crosshaircolor_b 0.00;
cl_crosshairalpha 180;
cl_crosshairdot 1;
cl_crosshairsize 1.5;
cl_crosshairgap -3;
cl_crosshairthickness 0;
cl_crosshair_outlinethickness 1;
cl_crosshair_drawoutline 0;
cl_crosshair_sniper_show_normal_inaccuracy 0;
cl_crosshair_t 0;
cl_crosshairgap_useweaponvalue 0;

cl_crosshair_sniper_width 0.5;

// settings
sensitivity 0.9;
zoom_sensitivity_ratio 1.0;
r_fullscreen_gamma 2.1;
rate 1000000;
safezonex "0.85"
safezoney "0.985"
cl_allow_animated_avatars "0"
cl_updaterate "128"
cl_use_opens_buy_menu "0"

// hud
cl_hud_background_alpha "0"
cl_hud_bomb_under_radar "1"
cl_hud_color "1"
cl_hud_healthammo_style "0"
cl_hud_playercount_pos "0"
cl_hud_playercount_showcount "1"
cl_hud_radar_scale "1.1"
hud_scaling "1"
hud_showtargetid "1"
hud_takesshots "0"

// radar
cl_radar_always_centered "1"
cl_radar_icon_scale_min "0.8"
cl_radar_rotate "1"
cl_radar_scale "0.35"
cl_radar_square_with_scoreboard "0"

// sound-related
speaker_config 1;

// others
cl_showfps 1;
